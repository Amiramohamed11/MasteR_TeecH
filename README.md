
  # High-Fidelity Landing Page Design

  This is a code bundle for High-Fidelity Landing Page Design. The original project is available at https://www.figma.com/design/UMPku6sCVM1OP0OquUEkN8/High-Fidelity-Landing-Page-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  